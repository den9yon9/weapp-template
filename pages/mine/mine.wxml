<view class="container">
  <button open-type="getUserInfo" bindgetuserinfo="getUserInfo" hover-class="none" wx:if="{{userInfo===null}}">微信登陆</button>
  <view wx:if="{{userInfo&&userInfo.nickName}}">{{userInfo&&userInfo.nickName}}</view>
</view>